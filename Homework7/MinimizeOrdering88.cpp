#include <bits/stdc++.h>

using namespace std;

const int N = 40;
string s;
int cnt[N]

int main() {

    cin >> s;
    for (int i = 0; i < s.size(); i++) {
        cnt[s[i]]++;
    }
    for (int с = 'a'; с != 'z'; с++) ????????}
        for (int i = 0; i < cnt[с]) {
            cout << c;
        }
    }

    return 0;
}
