#inlcude <bits/stdc++.h>

using namespace std;

const int N = 1004;
int n, a[N];

int main() {
    cin >> n;
    while (n--) {
        cin >> ai >> bi >> pi >> qi >> ri;
        int t = -bi/2*ai
        // А где взять l и r?;
    }
  
    return 0;
}
